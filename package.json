{
  "name": "kaden-vr-fishing-quest",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "db:setup": "node scripts/setup-db.js",
    "test": "node scripts/test-collisions.mjs && node scripts/test-fish-fight.mjs",
    "playtest": "node scripts/playtest-assets.mjs && node scripts/playtest-gameplay.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "three": "^0.170.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "vercel": "^41.1.0"
  }
}
